info:
  name: "Login (role: user)"
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/auth/login
  body:
    type: json
    data: |2-
        /* this user has secret 3 created for testing*/

      {
        "email":"teste@teste.teste",
        "password":"teste123!"  
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
