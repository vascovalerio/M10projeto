info:
  name: Register (new user)
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/auth/register
  body:
    type: json
    data: |-
      {
        "email":"registo1@teste.teste",
        "password":"teste123!"  
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
