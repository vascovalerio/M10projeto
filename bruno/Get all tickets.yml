info:
  name: Get all tickets
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/tickets
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
